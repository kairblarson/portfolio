import React from "react";

const Footer = () => {
    return <div>Footer</div>;
};

export default Footer;
//create footer
//add burger menu
//have the random buttons do something
//add form validation on the email form
//add success/failure prompts when email is send
//add a loading icon on the submit button when the promise is loading
//add a profile pic or something and my name on the navbar
